info:
  name: Games
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/games
  body:
    type: json
    data: '{"gameType":"tictactoe", "numberOfPlayers":4, "boardSize":4}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
