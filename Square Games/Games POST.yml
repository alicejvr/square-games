info:
  name: Games POST
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/games
  body:
    type: json
    data: '{"gameType":"Puissance 4", "numberOfPlayers":2, "boardSize":7}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
