info:
  name: Games GET
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/games
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
